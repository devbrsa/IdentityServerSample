@{
    ViewData["Title"] = "Home Page";
}


<div class="row">

    <div>
        <dl>
            @foreach (var claim in User.Claims)
            {
                <dt>@claim.Type</dt>
                <dd>@claim.Value</dd>
            }
        </dl>
    </div>
</div>
